# Copy to .env.development for local dev (npm run dev)
# Copy to .env.production for local production builds (npm run build)
# On Render, set VITE_API_URL in the service Environment tab instead.
VITE_API_URL=http://localhost:5000/api

